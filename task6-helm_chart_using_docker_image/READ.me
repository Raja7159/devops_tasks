# Create simple helm chart then edite value.yaml file file these details 

       - Image:
       - Imagepullpolicy:
       - tage:
       
 - then again install helm with new release name 
     
    - helm instal release.name ./helm.chart
